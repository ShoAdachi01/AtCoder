#include <bits/stdc++.h>
#include <iostream>
using namespace std;
#define rep(i,N) for (int i = 0; i < N; ++i)


int main() {
    int N, S;
    cin >> N;

    vector <int> A(N);
    rep(i,N) cin >> A[i];
    vector <int> dp(N + 1);

    rep(i,N) dp[i] = A[i];
    for (int i = 2; i <= N; ++i) {
        dp[i] = dp[]
    }



return 0;
}
